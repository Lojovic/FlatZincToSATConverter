var 0..50: x;
var -2..100: y;
var 1..80: z;
var -2..30: s;

constraint int_le(x, y);
constraint int_lt(x, z);
constraint int_ne(x, s);
constraint int_eq(y, z);
constraint int_min(z, s, y);
constraint int_max(x, y, z);
constraint int_plus(z, y, s);

solve satisfy;